---
# Runs on inventory hosts in debian_lxcs/existing_hosts with connection: local (see playbooks).
- name: Skip role when disabled
  ansible.builtin.meta: end_host
  when: not (lxc_host_config_enable | default(false) | bool)

- name: Build list of CT config lines to ensure on hypervisor
  ansible.builtin.set_fact:
    _lxc_host_config_lines: >-
      {{
        ([] if not (lxc_apparmor_unconfined_enable | bool) else [lxc_host_config_line_apparmor_unconfined])
        + ([] if not (lxc_apparmor_ubuntu_mask_enable | bool) else [lxc_host_config_line_apparmor_ubuntu_mask])
        + ([] if not (lxc_tun_enable | bool) else [lxc_host_config_line_tun_cgroup, lxc_host_config_line_tun_mount])
        + (lxc_host_config_extra_lines | default([]))
      }}

- name: Require at least one host-config line when role is enabled
  ansible.builtin.assert:
    that:
      - (_lxc_host_config_lines | length) > 0
    fail_msg: >-
      lxc_host_config_enable is true but no CT config lines were produced. Enable one or more of:
      lxc_apparmor_unconfined_enable, lxc_apparmor_ubuntu_mask_enable, lxc_tun_enable,
      or set lxc_host_config_extra_lines (e.g. mp0/mp1 bind mounts).

- name: Resolve host-specific metadata for Proxmox SSH patching
  ansible.builtin.set_fact:
    _lxc_host_config_vmid: >-
      {{
        [
          lxc_host_config_vmid | default(''),
          proxmox_lxc_vmid | default('')
        ]
        | map('string')
        | map('trim')
        | reject('equalto', '')
        | first
        | default('')
      }}
    _lxc_host_config_node_name: >-
      {{
        [
          lxc_host_config_node_name | default(''),
          proxmox_node_name | default('')
        ]
        | map('string')
        | map('trim')
        | reject('equalto', '')
        | first
        | default('')
      }}
    _lxc_host_config_node_address_map: >-
      {{
        lxc_host_config_node_addresses | default({})
      }}
    _lxc_host_config_ssh_user: >-
      {{
        [
          lxc_host_config_ssh_user | default(''),
          'root'
        ]
        | map('string')
        | map('trim')
        | reject('equalto', '')
        | first
      }}
    _lxc_host_config_ssh_private_key: >-
      {{
        [
          lxc_host_config_ssh_private_key_file | default(''),
          ansible_ssh_private_key_file | default('')
        ]
        | map('string')
        | map('trim')
        | reject('equalto', '')
        | first
        | default('')
      }}

- name: Resolve Proxmox node SSH target
  ansible.builtin.set_fact:
    _lxc_host_config_node_address: >-
      {{
        [
          lxc_host_config_node_address | default(''),
          _lxc_host_config_node_address_map.get(_lxc_host_config_node_name, ''),
          _lxc_host_config_node_name
        ]
        | map('string')
        | map('trim')
        | reject('equalto', '')
        | first
        | default('')
      }}

- name: Validate required metadata for host config patching
  ansible.builtin.assert:
    that:
      - (_lxc_host_config_vmid | string | length) > 0
      - (_lxc_host_config_vmid | int) > 0
      - (_lxc_host_config_node_address | string | length) > 0
      - (_lxc_host_config_ssh_user | string | length) > 0
    fail_msg: >-
      Missing metadata for {{ inventory_hostname }}. Set lxc_host_config_vmid and node target vars
      (lxc_host_config_node_address or lxc_host_config_node_name + lxc_host_config_node_addresses).

- name: Ensure bind-mount source directories exist on Proxmox node
  vars:
    _ssh_argv_head: >-
      {{
        ['ssh']
        + (
            ['-i', _lxc_host_config_ssh_private_key]
            if (_lxc_host_config_ssh_private_key | default('') | string | length) > 0
            else []
          )
        + ['-o', 'BatchMode=yes', '-o', 'StrictHostKeyChecking=' ~ (lxc_host_config_ssh_strict_host_key_checking | string)]
        + [_lxc_host_config_ssh_user ~ '@' ~ _lxc_host_config_node_address]
      }}
    _ensure_dir_program: |
      import grp
      import os
      from pathlib import Path
      import pwd
      import stat
      import sys

      path = Path(sys.argv[1])
      owner_arg = sys.argv[2].strip() if len(sys.argv) > 2 else ""
      group_arg = sys.argv[3].strip() if len(sys.argv) > 3 else ""
      mode_arg = sys.argv[4].strip() if len(sys.argv) > 4 else ""

      def resolve_uid(value: str):
          if not value:
              return None
          if value.isdigit():
              return int(value)
          return pwd.getpwnam(value).pw_uid

      def resolve_gid(value: str):
          if not value:
              return None
          if value.isdigit():
              return int(value)
          return grp.getgrnam(value).gr_gid

      changed = False
      if not path.exists():
          path.mkdir(parents=True, exist_ok=True)
          changed = True
      elif not path.is_dir():
          raise SystemExit(f"{path} exists but is not a directory")

      st = path.stat()
      target_uid = resolve_uid(owner_arg)
      target_gid = resolve_gid(group_arg)
      desired_uid = st.st_uid if target_uid is None else target_uid
      desired_gid = st.st_gid if target_gid is None else target_gid

      if st.st_uid != desired_uid or st.st_gid != desired_gid:
          os.chown(path, desired_uid, desired_gid)
          changed = True
          st = path.stat()

      if mode_arg:
          desired_mode = int(mode_arg, 8)
          current_mode = stat.S_IMODE(st.st_mode)
          if current_mode != desired_mode:
              os.chmod(path, desired_mode)
              changed = True

      if changed:
          print("changed")
  ansible.builtin.command:
    argv: "{{ _ssh_argv_head + ['python3', '-', item, (lxc_host_config_ensure_host_dirs_owner | default('') | string), (lxc_host_config_ensure_host_dirs_group | default('') | string), (lxc_host_config_ensure_host_dirs_mode | default('') | string)] }}"
    stdin: "{{ _ensure_dir_program }}"
  register: _lxc_host_config_ensure_dir
  changed_when: "'changed' in (_lxc_host_config_ensure_dir.stdout | default(''))"
  loop: "{{ lxc_host_config_ensure_host_dirs | default([]) }}"
  loop_control:
    label: "{{ item }}"
  when: (lxc_host_config_ensure_host_dirs | default([]) | length) > 0

- name: Check whether CT config exists on Proxmox node
  vars:
    _ssh_argv_head: >-
      {{
        ['ssh']
        + (
            ['-i', _lxc_host_config_ssh_private_key]
            if (_lxc_host_config_ssh_private_key | default('') | string | length) > 0
            else []
          )
        + ['-o', 'BatchMode=yes', '-o', 'StrictHostKeyChecking=' ~ (lxc_host_config_ssh_strict_host_key_checking | string)]
        + [_lxc_host_config_ssh_user ~ '@' ~ _lxc_host_config_node_address]
      }}
  ansible.builtin.command:
    argv: "{{ _ssh_argv_head + ['test', '-f', '/etc/pve/lxc/' ~ (_lxc_host_config_vmid | string) ~ '.conf'] }}"
  register: _lxc_host_config_conf_exists
  changed_when: false
  failed_when:
    - (_lxc_host_config_conf_exists.rc | int) != 0
    - not (ansible_check_mode | bool)

- name: Note skipped CT host config patch in check mode
  ansible.builtin.debug:
    msg: >-
      Skipping host-side CT config patch for VMID {{ _lxc_host_config_vmid }} in check mode:
      /etc/pve/lxc/{{ _lxc_host_config_vmid }}.conf does not exist yet.
      This is expected when lifecycle creation is planned but not applied.
  when:
    - ansible_check_mode | bool
    - (_lxc_host_config_conf_exists.rc | int) != 0

- name: Ensure CT config line present on Proxmox node
  vars:
    _ssh_argv_head: >-
      {{
        ['ssh']
        + (
            ['-i', _lxc_host_config_ssh_private_key]
            if (_lxc_host_config_ssh_private_key | default('') | string | length) > 0
            else []
          )
        + ['-o', 'BatchMode=yes', '-o', 'StrictHostKeyChecking=' ~ (lxc_host_config_ssh_strict_host_key_checking | string)]
        + [_lxc_host_config_ssh_user ~ '@' ~ _lxc_host_config_node_address]
      }}
    _ensure_line_program: |
      from pathlib import Path
      import sys

      conf = Path(sys.argv[1])
      line = {{ item | to_json }}
      key = line.split(":", 1)[0].strip()
      if not conf.exists():
          raise SystemExit(f"missing {conf} (CT may not exist on this node)")
      content = conf.read_text(encoding="utf-8")
      existing_lines = content.splitlines()
      normalized_lines = [
          existing
          for existing in existing_lines
          if existing.strip() not in {f"{key}:", f"{key}: "}
      ]
      if line not in normalized_lines:
          normalized_lines.append(line)
      if normalized_lines != existing_lines:
          conf.write_text("\n".join(normalized_lines) + "\n", encoding="utf-8")
          print("changed")
  ansible.builtin.command:
    argv: "{{ _ssh_argv_head + ['python3', '-', '/etc/pve/lxc/' ~ (_lxc_host_config_vmid | string) ~ '.conf'] }}"
    stdin: "{{ _ensure_line_program }}"
  register: _lxc_host_config_append
  changed_when: "'changed' in (_lxc_host_config_append.stdout | default(''))"
  loop: "{{ _lxc_host_config_lines }}"
  loop_control:
    label: "{{ item }}"
  when: (_lxc_host_config_conf_exists.rc | int) == 0

- name: Restart CT on hypervisor when its config changed and restart is allowed
  vars:
    _ssh_argv_head: >-
      {{
        ['ssh']
        + (
            ['-i', _lxc_host_config_ssh_private_key]
            if (_lxc_host_config_ssh_private_key | default('') | string | length) > 0
            else []
          )
        + ['-o', 'BatchMode=yes', '-o', 'StrictHostKeyChecking=' ~ (lxc_host_config_ssh_strict_host_key_checking | string)]
        + [_lxc_host_config_ssh_user ~ '@' ~ _lxc_host_config_node_address]
      }}
    _restart_program: |
      import subprocess
      import sys

      vmid = sys.argv[1]
      status = subprocess.run(["pct", "status", vmid], check=False, capture_output=True, text=True)
      if status.returncode != 0:
          raise SystemExit(status.stderr.strip() or status.stdout.strip() or f"pct status failed for {vmid}")
      if "running" in status.stdout.lower():
          subprocess.run(["pct", "stop", vmid], check=True)
          subprocess.run(["pct", "start", vmid], check=True)
          print("changed")
  ansible.builtin.command:
    argv: "{{ _ssh_argv_head + ['python3', '-', (_lxc_host_config_vmid | string)] }}"
    stdin: "{{ _restart_program }}"
  when:
    - lxc_host_config_restart | default(false) | bool
    - (_lxc_host_config_append.results | default([]) | selectattr('changed', 'equalto', true) | list | length) > 0
  changed_when: "'changed' in (_lxc_host_config_restart.stdout | default(''))"
  register: _lxc_host_config_restart
