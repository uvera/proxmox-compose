- name: Reconcile Proxmox infrastructure from inventory metadata
  hosts: localhost
  gather_facts: false
  connection: local
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  roles:
    - role: proxmox_lifecycle
